/**
 * 모든 할 일을 조회한다.
 */
function findAll () {}

/**
 * id를 기반으로 할 일을 조회한다.
 * @param id {string}
 */
function findById (id) {}

/**
 * todoId와 tagId를 기반으로 tag 를 리턴한다.
 * @param todoId {string}
 * @param tagId {string}
 * @return {tag}
 */
function findTagById (todoId, tagId) {}