/**
 * 모든 할 일을 삭제한다.
 */
function deleteAll () {}

/**
 * id를 기반으로 할 일을 삭제한다.
 * @param todoId {string}
 */
function deleteById (todoId) {}

/**
 * id로 할 일을 찾아 tags 를 tag.id를 기반으로 조회하여 tagName 을 삭제한다.
 * @param todoId {string}
 * @param tag {tag}
 */
function deleteTagById (todoId, tag) {}

/**
 * id를 기반으로 할 일을 찾아 모든 태그를 삭제한다.
 * @param todoId {string}
 */
function deleteAllTagById (todoId) {}